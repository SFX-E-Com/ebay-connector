# ============================================
# eBay Connector - Environment Variables
# ============================================

# ----------------------------------------
# Google Cloud / Firestore
# ----------------------------------------
# GCP Projekt ID (für Firestore)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Service Account Key (JSON-Pfad für lokale Entwicklung)
# In Cloud Run wird dies automatisch über den Service Account gehandhabt
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# ----------------------------------------
# eBay API Credentials
# ----------------------------------------
# eBay Developer Account: https://developer.ebay.com/
# Use Sandbox credentials when EBAY_SANDBOX=true
# Use Production credentials when EBAY_SANDBOX=false
EBAY_CLIENT_ID=your-ebay-client-id
EBAY_CLIENT_SECRET=your-ebay-client-secret
EBAY_DEV_ID=your-ebay-dev-id

# OAuth Redirect URI (must match in eBay Developer Portal)
EBAY_REDIRECT_URI=http://localhost:3000/api/ebay/oauth/callback

# Sandbox Mode (true for testing, false for production)
EBAY_SANDBOX=true

# Default eBay Marketplace
# Options: EBAY_US, EBAY_DE, EBAY_GB, EBAY_FR, etc.
EBAY_DEFAULT_MARKETPLACE=EBAY_DE

# OAuth Scopes (space-separated)
# Required scopes for messaging, orders, inventory, etc.
EBAY_OAUTH_SCOPES=https://api.ebay.com/oauth/api_scope https://api.ebay.com/oauth/api_scope/sell.inventory https://api.ebay.com/oauth/api_scope/sell.fulfillment https://api.ebay.com/oauth/api_scope/sell.account

# ----------------------------------------
# Authentication
# ----------------------------------------
# JWT Secret für Token-Signierung (generieren mit: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-min-32-chars

# Auth URL (für Callbacks)
AUTH_URL=http://localhost:3000

# ----------------------------------------
# App Configuration
# ----------------------------------------
# Next.js
NODE_ENV=development
PORT=3000

# ----------------------------------------
# Optional: Testing Configuration
# ----------------------------------------
# Test eBay Account ID (from Firestore after OAuth connection)
TEST_EBAY_ACCOUNT_ID=

# Test User Credentials for E2E tests
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Test IDs for messaging E2E tests
TEST_MESSAGE_ID=
TEST_ORDER_ID=

# ----------------------------------------
# Optional: Additional Configuration
# ----------------------------------------
# Enable debug logging
DEBUG=false

# Base URL for public-facing URLs
NEXT_PUBLIC_BASE_URL=http://localhost:3000