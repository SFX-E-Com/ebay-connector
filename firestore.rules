rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For testing purposes - allow all in emulator
    // In production, these rules should be more restrictive
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
