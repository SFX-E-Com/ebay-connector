# ============================================
# eBay Connector - Production Environment Variables
# ============================================
# This file is a template for Cloud Run environment variables
# DO NOT commit actual credentials to Git!

# ----------------------------------------
# Google Cloud / Firestore
# ----------------------------------------
GOOGLE_CLOUD_PROJECT=sharkyv0
FIRESTORE_DATABASE_ID=ebay-connector

# Note: GOOGLE_APPLICATION_CREDENTIALS is NOT needed in Cloud Run
# Cloud Run uses the default service account automatically

# ----------------------------------------
# eBay API Credentials (SANDBOX for testing)
# ----------------------------------------
# Get these from: https://developer.ebay.com/my/keys
EBAY_CLIENT_ID=YOUR_EBAY_CLIENT_ID
EBAY_CLIENT_SECRET=YOUR_EBAY_CLIENT_SECRET
EBAY_DEV_ID=YOUR_EBAY_DEV_ID

# IMPORTANT: Set to true for Sandbox testing
EBAY_SANDBOX=true

# Default eBay Marketplace
EBAY_DEFAULT_MARKETPLACE=EBAY_DE

# OAuth Scopes (space-separated URLs)
EBAY_OAUTH_SCOPES=https://api.ebay.com/oauth/api_scope https://api.ebay.com/oauth/api_scope/sell.inventory https://api.ebay.com/oauth/api_scope/sell.fulfillment https://api.ebay.com/oauth/api_scope/sell.account

# ----------------------------------------
# OAuth Redirect URLs
# ----------------------------------------
# REPLACE WITH YOUR ACTUAL CLOUD RUN URL!
# Example: https://ebay-connector-abc123-ew.a.run.app
EBAY_REDIRECT_URI=https://YOUR-CLOUD-RUN-URL.run.app/api/ebay/oauth/callback
AUTH_URL=https://YOUR-CLOUD-RUN-URL.run.app

# ----------------------------------------
# Authentication
# ----------------------------------------
# IMPORTANT: Generate a new secret for production!
# Run: openssl rand -base64 32
JWT_SECRET=GENERATE_NEW_SECRET_FOR_PRODUCTION

# ----------------------------------------
# App Configuration
# ----------------------------------------
NODE_ENV=production
PORT=8080
NEXT_TELEMETRY_DISABLED=1

# ----------------------------------------
# Optional: Debug Settings
# ----------------------------------------
DEBUG=false
NEXT_PUBLIC_BASE_URL=https://YOUR-CLOUD-RUN-URL.run.app

# ----------------------------------------
# Migration Notes
# ----------------------------------------
# When ready to switch to PRODUCTION eBay:
# 1. Change EBAY_SANDBOX=false
# 2. Update EBAY_CLIENT_ID to production credentials
# 3. Update EBAY_CLIENT_SECRET to production credentials
# 4. Create NEW RuName in eBay Production Developer Portal
# 5. Update EBAY_REDIRECT_URI with production RuName
# 6. Test thoroughly in production environment
